VERSION 0.5.0
- Simplify PhenoPro() function, separate cross-validation and plot option;
- Add predict option;
- Add a default clustering method K-means;
- Improve predict() function that allows Pheno object;
- Create cv() function that allows functioning on Pheno object;
- Improve plot() function that allows plotting cvPheno object;
- Improve DESCRIPTION file, Imports: viridis, ggthemes;
- Improve NAMESPACE file, import(viridis), ggthemes.

VERSION 0.4.1
- Fix a bug that return of "mapply" is not a list when test sample size are equal, testSample <- mapply(...,SIMPLIFY = FALSE)

VERSION 0.4.0
- Delete CheckBlockStatus() function;
- Improve the return of unique() function;
- Improve SplitData() function;
- Improve BayesianNIGcv() function;
- Add inverseLabel argument that might have multiple components;
- Improve BayesianNIGdirect() function;
- Improve PhenoPro() function that allows number of label can be larger than 2;
- Improve Predict() function while still remain some problems.

VERSION 0.3.1
- Improve block argument that allows NULL or two columns as input;
- Improve orderby argument that allows NULL as input;
- Add built-in CheckInputArguments() function; 
- Add built-in TransferData() function;
- Improve DESCRIPTION file, Imports: gtools;
- Improve NAMESPACE file, mixedsort(); 
- Add a new dataset named sds.rda which block and label have two and three components;

VERSION 0.3.0
- Add visualization argument;
- Add process to check visualization argument;
- Improve DESCRIPTION file, Imports: ggplot2, gridExtra;
- Improve NAMESPACE file, grid.arrange() and import(ggplot2); 
- Fix a bug that impacts on variable orders.

VERSION 0.2.2
- Improve output in which matrix are associated with their names. 

VERSION 0.2.1
- Fix informal argument that loads dataset sds;
- Improve DESCRIPTION file, Imports: MASS, e1071, randomForest;
- Improve NAMESPACE file, ginv(), svm(), randomForest();
- Add readNews() to display changes in new version.

VERSION 0.2.0
- Improve DESCRIPTION file, including sections of Title, Description, LazyData, Depends;
- Add stop argument for error report;
- Add a dataset named sds.rda and its documents;
- Add an argument for printing current version;
- Add step argument;
- Add dependent packages;
- Add new licence;
- Add x and y arguments for multiple columns calculating.

VERSION 0.1.0
- Add the main function PhenoPro() to yield performance, recall and precision for each block.
